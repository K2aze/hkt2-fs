import { RegisterForm } from "@/features/auth";

const page = () => {
  return (
    <div className="grid place-items-center min-h-svh pt-20">
      <RegisterForm />
    </div>
  );
};

export default page;
