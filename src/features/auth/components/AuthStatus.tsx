import { Button } from "@/components/ui/button";
import styles from "./AuthStatus.module.scss";

const AuthStatus = () => {
  return <Button className={styles.btn}>Guess</Button>;
};

export default AuthStatus;
